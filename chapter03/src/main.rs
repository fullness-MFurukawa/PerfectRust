//!
//! 3章　変数と定数
//! 
mod variable;
mod constant;
mod scope;
mod static_val;
fn main() {
    // variable::declar_mutable_variables(); // ミュータブルな変数の宣言
    // variable::shadowing(); // シャドーイング
    // constant::use_constant();// 定数の宣言と利用
    // scope::blcok_and_scope();// ブロックとスコープ
    // let result = static_val::calc_amount(100); // 静的変数の宣言と利用
    // println!("単価:100の税込み金額={}" , result);
    // static_val::calc_total(100); // ミュータブルな静的変数の利用
}
